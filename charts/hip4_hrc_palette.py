"""HRC dark palette for the HIP-4 chart set.

Text roles follow the HRC dark spec; the series keep the reference hues
(mint / purple / indigo) so the four charts stay readable as one set.
"""
TITLE = '#D9D9D9'   # headline / annotation text
BODY = '#9CA3AF'    # axis ticks, sub-labels
NOTE = '#6D6D6D'    # note, muted annotation

HYPERLIQUID = '#5058DE'  # Hyperliquid canonical markets
OUTCOME = '#78DEC9'
SKEW = '#A78BFA'
MUTED_BAR = '#4E525C'    # non-highlighted bars
